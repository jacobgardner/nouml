import noteIcon from './note-icon.svg';

// const parser = new DOMParser();
// const doc = parser.parseFromString(noteIcon, 'image/svg+xml');

// export function mountSymbols(
//   svgRoot: d3.Selection<SVGSVGElement, unknown, null, undefined>
// ) {


//   svgRoot.append('image').attr('xlink:href', noteIcon).attr('x', 50).attr('width', 10).attr('height', 10);
//   svgRoot.append('image').attr('xlink:href', noteIcon).attr('width', 500);
//   svgRoot.append('image').attr('xlink:href', noteIcon);
// }
